import React, {useState, useEffect} from 'react'
import { fetchCards, searchCardByName  } from '../services/cardApi'

export default function Test() {
    const [cards, setCards] = useState([])
  return (
    <div>Test</div>
  )
}
